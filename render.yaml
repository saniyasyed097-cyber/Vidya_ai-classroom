services:
  - type: web
    name: vidya-ai-classroom
    env: python
    buildCommand: "pip install -r requirements.txt && python backend/build_offline_db.py"
    startCommand: "uvicorn backend.app:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: PYTHON_VERSION
        value: 3.13.0
